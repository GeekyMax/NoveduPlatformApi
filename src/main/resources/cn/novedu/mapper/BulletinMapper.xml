<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.novedu.mapper.BulletinMapper">
    <!--auto generated Code-->
    <resultMap id="BaseResultMap" type="cn.novedu.bean.Bulletin">
        <result column="id" property="id" jdbcType="VARCHAR"/>
        <result column="clazz_id" property="clazzId" jdbcType="VARCHAR"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--auto generated Code-->
    <sql id="Base_Column_List">
        id,
        clazz_id,
        title,
        content,
        create_time
    </sql>

    <!--auto generated Code-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="bulletin.id">
        <selectKey keyProperty="bulletin.id" order="BEFORE" resultType="string">
            select replace(uuid(),'-','') from dual;
        </selectKey>
        INSERT INTO bulletin (
        id,
        clazz_id,
        title,
        content
        ) VALUES (
        #{bulletin.id,jdbcType=VARCHAR},
        #{bulletin.clazzId,jdbcType=VARCHAR},
        #{bulletin.title,jdbcType=VARCHAR},
        #{bulletin.content,jdbcType=VARCHAR}
        )
    </insert>

<!--auto generated by codehelper on 2018-12-11-->
	<select id="findByClazzId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from bulletin
        where clazz_id=#{clazzId,jdbcType=VARCHAR}
    </select>
</mapper>

